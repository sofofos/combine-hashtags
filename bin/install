#!/usr/bin/env ruby

# executable to query the api and get json data stored locally in rails

require "dotenv/load"
require "combine_hashtags"
require "combine_hashtags/query"


@query = CombineHashtags::Query.new

@query.call_api

puts "files loaded at #{ENV["TEST_STORAGE_PATH"]}"