#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
echo "to set your API access token, run set-keys.sh script in the config/ directory

